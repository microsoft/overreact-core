const {
  makeSpecMetadata,

  specMetadataScope,
  specMetadataType,
} = require('./src/odata-gen');

module.exports = {
  makeSpecMetadata,

  specMetadataScope,
  specMetadataType,
};
