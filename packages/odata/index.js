const {
  makeSpecMetadata,
  makeSchemaModel,

  specMetadataScope,
  specMetadataType,
} = require('./src/odata-gen');

module.exports = {
  makeSpecMetadata,
  makeSchemaModel,

  specMetadataScope,
  specMetadataType,
};
