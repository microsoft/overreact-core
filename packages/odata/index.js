const {
  makeSpecMetadata,
  makeSchemaModel,
  createSpecList,

  specMetadataScope,
  specMetadataType,
} = require('./src/odata-gen');

module.exports = {
  makeSpecMetadata,
  makeSchemaModel,
  createSpecList,

  specMetadataScope,
  specMetadataType,
};
